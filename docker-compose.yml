version: '2'

services:
  web:
    restart: always
    build: .
    expose:
      - "3011"
    volumes:
      - /usr/src/app/public:/src/public:rw
    environment:
      - VIRTUAL_HOST=interline.in,www.interline.in
      - LETSENCRYPT_HOST=interline.in,www.interline.in
      - LETSENCRYPT_EMAIL=info@interline.in
      - LETSENCRYPT_TEST=true
      - NODE_ENV=production
    command: /src/startup.sh
    networks:
      - proxy
      - default

networks:
  proxy:
    external:
      name: proxy
